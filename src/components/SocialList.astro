---
import type { TSocialList } from "../lib/types";
type Props = TSocialList;

const { list } = Astro.props;
---

<div class="socials">
    {
        list.map((social) => (
            <a href={social.link} rel="noreferrer">
                {social.text}
            </a>
        ))
    }
</div>
